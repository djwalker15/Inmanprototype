
  # InMan-Prototype

  This is a code bundle for InMan-Prototype. The original project is available at https://www.figma.com/design/qKSvtERBHFkrhaXPnAfPR2/InMan-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  